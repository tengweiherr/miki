{
  "name": "miki-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter www dev",
    "build": "pnpm --filter www build",
    "start": "pnpm --filter www start",
    "lint": "pnpm -r lint",
    "build:miki": "pnpm --filter miki build"
  }
}
